# Qwen600 HPC 优化题目与验收规则

## 任务

在不超出既定数值误差的前提下，提高 Qwen/Qwen3-0.6B 完整推理请求的性能。
保留单 GPU、batch=1、并发=1，不把批处理或多并发改造计入本题。
从原始 V0 开始，通过代码与性能分析提出假设、实现、验证、测量和决策。
不规定必须优化哪个 kernel，也不要求采用与教师参考版相同的优化方法。

## 硬件、模型与本机参考

学生自行准备满足项目依赖的硬件与环境，使用 [README](README.md) 指定的 Qwen/Qwen3-0.6B 版本。
首先在尚未优化的 V0 上生成本机参考包，记录 manifest SHA256；随后整项优化使用同一个包。
参考历史为本机 V0 的续写，比较时各版本使用相同 prompt 和后续 token 历史，不采用候选自己的续写替代历史。

只比较同一台机器、同一 GPU 配置、软件环境和计时设置下的 V0 与候选。
文档记录 CPU/GPU、显存容量、驱动、CUDA、编译器、构建模式/架构和 Python 版本；
同时记录运行中是否有其他 GPU 任务及功耗/频率设置。编译选项若作为优化项调整，应写明前后差异。
不得把不同设备上的绝对时间直接当作优化收益或学生排名。

换硬件、驱动或工具链后，需在该环境重新生成未优化 V0 参考，并重新测量 V0 与候选。
老师的参考实现用于分析方法与优化效果的讨论，其其他硬件上的现成成绩不是本机性能门槛。

## 正确性门槛：完整模型输出

默认使用100条固定 ShareGPT，在每条6个选定位置比较完整151936维 logits，共600个向量。
每个位置必须有限且同时满足：

| 指标 | 上限 |
| --- | ---: |
| logits 平均绝对误差 | 0.05 |
| logits 最大绝对误差 | 0.5 |
| softmax 概率总变差 TV | 0.02 |
| V0 对候选 top-1 的分数损失 | 0.125 |

开发时可用固定六条/36个位置；阶段完成及最终提交使用完整100条检查。
验收真实推理路径的最终 logits，不把独立算子或 hidden states 检查作为门槛。
允许融合、布局变化和消除中间张量；算子、逐层或 sanitizer 诊断可用于解释问题，不单独计分。
本题是相对本机 V0 的数值约束，不要求候选与不同平台逐位相同。

## 性能验收：完整请求

默认负载为固定 ShareGPT100；输入和输出 token 数量不变，所有阶段均使用 protocol 4、ITL开启、不采显存。
每进程真实预热一次、每条正式测量一次，作为统一的阶段测量设置。
如果追加重复测量，比较双方须使用相同重复次数和预热设置；小幅收益要说明波动，不能只挑最好的一次。
正式计时不与 Nsight 或 sanitizer 同跑，profiling 数据只用于分析原因。
fixed9 可辅助研究长度敏感性，不是额外必交集合。

主要结果是相同工作负载的总请求耗时与吞吐；同时记录 Prefill、TTFT、TPOT、ITL、
请求总耗时的均值与 P50/P95/P99，以及输入输出合计、全请求输出、Decode、串行请求吞吐。
串行请求吞吐是请求数除以请求计时总和，不表示多并发承载能力。

- 加速比 = V0总请求耗时 / 候选总请求耗时。
- 耗时降低比例 = (V0总请求耗时 − 候选总请求耗时) / V0总请求耗时。
- 每阶段同时区分相对上一阶段的收益与相对初始V0的累计收益，不能将各项百分比直接相加。

不得修改模型权重、数值阈值、测试输入、输出预算或计时口径来获取成绩。
不得缓存测试用例答案、识别固定用例走捷径，或把必要推理工作搬到计时区间外。
若优化改变前向接口，可适配探针与计时器的调用入口，必须说明变更并保持相同计时边界、工作量与真实计算路径。

## 提交：一份优化过程文档

唯一必交材料为一份过程文档，可用 Word、PDF 或包含截图的 Markdown；不额外要求提交代码仓库。
使用 [模板](docs/REPORT_TEMPLATE.md)，完整写出以下内容：

1. 硬件软件环境、模型版本、V0版本、本机参考manifest哈希与未优化基线。
2. 每个优化阶段的 Observation、Hypothesis、Implementation、Correctness、Benchmark、Re-profile、Decision。
3. 对失败或退化方案的判断，解释采用、撤回或保留的原因。
4. 最终完整正确性结果、与V0的整体性能对比，以及已知限制。

每个已采用阶段附正确性结果和整体benchmark截图；迭代中的快检/短测标注“quick”，不能冒充完整验收。
截图要能对应阶段、执行命令/参数和结果，不裁掉失败项或混用不同版本。
核心数值也填写在汇总表中，不只贴图。关键实现可用代码片段或diff说明。
不要求通过单个kernel加速来证明整项优化合格，最终看完整推理结果与整体收益。

老师根据文档审查方法、因果解释、测量条件、整体收益与退化说明；截图和文字属于学生提供的实验记录，
不自动等同教师独立复测结论。学生可在本机保留源码与原始结果以便答辩说明。
