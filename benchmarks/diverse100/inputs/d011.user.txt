Summarize these system design notes and identify the main tradeoffs.
Record 11.1: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.2: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.3: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.4: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.5: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.6: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.7: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.8: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.9: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.10: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.11: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.12: The service receives requests through a queue. Workers read immutable configuration and write results to a separate store. A bounded queue limits memory use, but a small capacity can increase waiting time. Record latency and failures separately. Recovery must preserve completed work without processing the same item twice.
Record 11.13: The service receives requests through a queue. Workers read immutable configuration and write results to a